# Server
PORT=3000
NODE_ENV=production

# SMTP for email alerts (optional - without it, emails are logged to console)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=alert@heartbeat.hermes.run
